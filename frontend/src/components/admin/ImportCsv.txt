return (
    <div style={{ padding: '20px', border: '2px solid #007bff', borderRadius: '8px', background: '#fff', marginTop: '20px' }}>
      <h3 style={{ color: '#333', marginBottom: '10px' }}>Công cụ nạp 964 người</h3>
      
      {/* 1. Ô chọn file */}
      <input 
        type="file" 
        accept=".csv" 
        onChange={handleFileChange} 
        style={{ display: 'block', marginBottom: '20px', padding: '10px', border: '1px solid #ccc', width: '100%' }} 
      />
      
      {/* 2. Nút bấm - Tôi đã thêm màu Đỏ để bạn dễ thấy */}
      <button 
        onClick={handleUpload} 
        disabled={loading}
        style={{
          display: 'block',
          width: '100%',
          padding: '15px',
          backgroundColor: loading ? '#666' : '#ff4d4f', // Màu đỏ nổi bật
          color: 'white',
          fontSize: '18px',
          fontWeight: 'bold',
          border: 'none',
          borderRadius: '5px',
          cursor: loading ? 'not-allowed' : 'pointer',
          boxShadow: '0 4px 6px rgba(0,0,0,0.1)'
        }}
      >
        {loading ? 'ĐANG XỬ LÝ 964 NGƯỜI...' : 'BẤM VÀO ĐÂY ĐỂ IMPORT'}
      </button>

      {/* 3. Thông báo trạng thái */}
      {message && (
        <div style={{ marginTop: '20px', padding: '10px', background: '#e6f7ff', border: '1px solid #91d5ff', borderRadius: '4px' }}>
          <strong>Thông báo:</strong> {message}
        </div>
      )}
    </div>
  );